name: Variables Demo

on:
  push

env:
  APP_NAME: Portfolio
  ENVIRONMENT: Dev

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: Print Variables
        run: |
          echo "Application: $APP_NAME"
          echo "Environment: $ENVIRONMENT"
